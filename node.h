#ifndef AUTOGRAD_NODE_H
#define AUTOGRAD_NODE_H

#include <string>
#include <string_view>


class Node {

  public:
    Node() = default;

    /* Add operator+(const Node& other) */
    /* { */
    /*   return Add(*this, other); */
    /* } */
};

#endif // AUTOGRAD_NODE_H
